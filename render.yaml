services:
  - type: web
    name: autonomous-desktop-agent
    runtime: python
    buildCommand: pip install -r requirements-cloud.txt
    startCommand: uvicorn web.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    autoDeploy: true
